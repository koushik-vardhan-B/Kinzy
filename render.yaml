services:
  - type: web
    name: ai-game-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"
